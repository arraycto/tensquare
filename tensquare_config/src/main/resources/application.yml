server:
  port: 12000
spring:
  application:
    name: tensquare-config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/flypaopao/tensquare54.git
  rabbitmq:
    host: 192.168.73.140
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh
